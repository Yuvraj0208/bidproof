# apps/web

React + TypeScript + Tailwind + shadcn/ui frontend — the ten screens
(SPEC §17), pdf.js for click-to-proof highlighting, TipTap for the proposal
editor.

Scaffolded with the first UI story (US-13: the confidence chip, Week 2) so
the whole frontend toolchain lands in one piece.
