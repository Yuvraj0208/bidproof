[project]
name = "bidproof-proposalwriter"
version = "0.1.0"
description = "ProposalWriter agent: section drafts with enforced source tags"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bidproof_proposalwriter"]
