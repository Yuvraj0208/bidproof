[project]
name = "bidproof-riskscorer"
version = "0.1.0"
description = "RiskScorer agent: dangerous clauses, priced in rupees by plain arithmetic"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bidproof_riskscorer"]
