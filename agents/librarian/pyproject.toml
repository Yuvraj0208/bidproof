[project]
name = "bidproof-librarian"
version = "0.1.0"
description = "Librarian agent: past proposals as tagged, outcome-ranked reusable blocks"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bidproof_librarian"]
