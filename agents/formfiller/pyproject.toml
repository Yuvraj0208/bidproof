[project]
name = "bidproof-formfiller"
version = "0.1.0"
description = "FormFiller agent: standard declarations filled from real data only"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bidproof_formfiller"]
