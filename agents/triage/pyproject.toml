[project]
name = "bidproof-triage"
version = "0.1.0"
description = "Triage agent: deterministic fit scoring and two-list assignment"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bidproof_triage"]
