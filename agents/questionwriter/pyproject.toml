[project]
name = "bidproof-questionwriter"
version = "0.1.0"
description = "QuestionWriter agent: grounded, cited pre-bid query letters — drafts only"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bidproof_questionwriter"]
