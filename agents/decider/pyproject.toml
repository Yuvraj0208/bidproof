[project]
name = "bidproof-decider"
version = "0.1.0"
description = "Decider agent: expected value in rupees, deterministic, human-gated"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bidproof_decider"]
