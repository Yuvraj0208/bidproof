# Binary assets — never line-ending-converted.
*.pdf binary
*.png binary
*.jpg binary

# Files that run inside the Linux container must stay LF on every checkout.
*.sh text eol=lf
Dockerfile text eol=lf
infra/deploy/Caddyfile text eol=lf
